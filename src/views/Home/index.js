import Chart from "../../components/Chart";
import Table from "../../components/Table";

const Home = () => {
  return `<home>
            ${Table()}
            ${Chart()}
          </home>`;
};

export default Home;
